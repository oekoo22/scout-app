# Scout App Environment Configuration

# Environment (development, production)
SCOUT_ENV=development

# Server configuration
SCOUT_HOST=0.0.0.0
SCOUT_PORT=8000

# Network configuration
# Set to true to use local network IP for mobile testing
SCOUT_USE_LOCAL_IP=false

# Additional CORS origins (comma-separated)
SCOUT_CORS_ORIGINS=

# Debug mode
SCOUT_DEBUG=true

# Google OAuth Configuration
# The redirect URI is now automatically set based on SCOUT_USE_LOCAL_IP and SCOUT_OAUTH_HOST
# For development (default): http://localhost:8000/auth/google/callback  
# For mobile testing: http://YOUR_LOCAL_IP:8000/auth/google/callback

# Override the OAuth host (optional)
# SCOUT_OAUTH_HOST=localhost
# SCOUT_OAUTH_HOST=192.168.1.100

# Override the complete OAuth redirect URI (optional)
# SCOUT_OAUTH_REDIRECT_URI=http://localhost:8000/auth/google/callback

# IMPORTANT: Make sure to add ALL redirect URIs to your Google Cloud Console:
# 1. Go to Google Cloud Console -> APIs & Services -> Credentials
# 2. Edit your OAuth 2.0 Client ID
# 3. Add these Authorized redirect URIs:
#    - http://localhost:8000/auth/google/callback (for development)
#    - http://YOUR_LOCAL_IP:8000/auth/google/callback (for mobile testing)
# 4. Replace YOUR_LOCAL_IP with your actual local network IP address